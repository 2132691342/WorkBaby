# DESIGN.md · 界面设计规范

> 视觉语言唯一权威。色值与尺寸只在 `frontend/src/src/themes.css` 定义，组件语法唯一实现在 `frontend/src/src/wb-ui.css`。
> 可交互原型见 [`docs/prd/prototype/index.html`](docs/prd/prototype/index.html)（40 屏，双击打开）。

## 1. 设计方向

一句话：**纯色 + 镂空 + 1px 中性描边**。

界面只允许「主色 + 灰阶 + 白」。绿 / 红只作状态灯（通过 / 出错），不参与装饰。

三条纪律，对应「看上去廉价」的三个根因：

| # | 纪律 | 原因 |
|---|---|---|
| 1 | 背景是**纯色**，不做多色光晕 | 光晕是「糖纸感」的直接来源 |
| 2 | 分层靠「1px 中性描边 + 极轻阴影」 | 大模糊与彩色投影是廉价感的来源 |
| 3 | 颜色只出现在必须被看到的地方：主操作、当前项、状态灯 | 六色混搭会让语义与装饰互相稀释 |

## 2. 主题

两套主题共用同一组语义令牌（`--wb-*`），组件不写死色值——这是「暗色漏白」的根治手段。

| 主题 | 令牌块 | 底色 | 面 | 强调色 |
|---|---|---|---|---|
| 浅色 · 晴空 | `:root` / `[data-theme='light']` | `#f2f6fc` 冷白 | `#ffffff` 纯白卡 | `#2f80ed` 亮蓝 |
| 暗色 · 紫夜 | `[data-theme='dark']` | 近黑 | 深灰紫卡 | `#7c5cf8` 紫罗兰 |

`useTheme.ts` 的 THEMES 表是设置页预览视图，取值必须与 CSS 同步。

## 3. 设计令牌

### 3.1 字体三族

| 令牌 | 用途 |
|---|---|
| `--font-disp`（Bahnschrift / Segoe UI Variable Display） | 标题与读数（DIN 风） |
| 系统无衬线（默认） | UI 正文 |
| JetBrains Mono / 等宽 | 路径、ID、token、代码 |

不打包 Inter。

### 3.2 字号 6 级

`--wb-fs-xs`(11) / `--wb-fs-sm`(12) / `--wb-fs-md`(13) / `--wb-fs-lg`(15) / `--wb-fs-xl`(20) / `--wb-fs-2xl`(28)

Tailwind 刻度类：`text-3xs`(9) / `text-2xs`(11) / `text-xs2`(12) / `text-ctl`(12.5)。**禁止 `text-[Npx]` 任意值。**

### 3.3 间距（4 的倍数）

`--wb-sp-1`(4) … `--wb-sp-12`(48)，步进 4 / 8 / 12 / 16 / 20 / 24 / 32 / 40 / 48。

### 3.4 圆角 4 级 + 胶囊

`--wb-radius-xs`(6) / `-sm`(8) / `-`(10) / `-lg`(16) / `-xl`(20) / `-full`(999)。
圆润靠圆角与阴影，不靠描边。

### 3.5 控件高度 4 档 + 图标钮

| 令牌 | 值 | 用途 |
|---|---|---|
| `--wb-ctl-h-sm` | 26px | 芯片 / 分段控件 |
| `--wb-ctl-h` | 34px | 按钮 / 输入 |
| `--wb-ctl-h-lg` | 38px | 侧栏导航 |
| `--wb-ctl-h-xl` | 44px | 主操作 |
| `--wb-ctl-icon` | 32px | 圆形图标按钮 |

**禁止裸像素高度。**

### 3.6 动效 3 档

`--wb-dur-fast`(110ms) / `--wb-dur`(160ms) / `--wb-dur-slow`(240ms)，缓动 `--wb-ease: cubic-bezier(0.2, 0.6, 0.3, 1)`。
**只解释状态变化，不做装饰。**

### 3.7 布局宽度与层级

| 令牌 | 值 |
|---|---|
| `--wb-w-nav` | 236px 侧栏 |
| `--wb-w-settings` | 208px 设置左导航 |
| `--wb-w-panel` | 336px 右栏 |
| `--wb-w-panel-wide` | 420px 右栏（宽） |
| `--wb-w-read` | 780px 正文阅读宽度 |

`--wb-z-panel`(20) / `--wb-z-pop`(60) / `--wb-z-modal`(80) / `--wb-z-toast`(100)

## 4. 组件语法

| 组件 | 规则 |
|---|---|
| 按钮 | **一律胶囊镂空**（描边 + 主色/中性字），实底只出现在 hover |
| 卡片 | 不透明底 + 1px 中性描边 + 极轻阴影 |
| 输入 | 聚焦是 1px 主色 + 3px 淡光 |
| 标签 | 降噪为灰底灰字（不做彩色标签墙） |
| 分段控件 | 选中是「抬起的白片」，非彩色块 |
| 状态灯 | 去掉彩色光晕 |
| 图标 | Lucide 风格，24 网格，统一笔画 `--wb-icon-stroke`(1.7) |
| 空态 / 骨架 | 统一 `EmptyState` / `Skeleton`；列表页经 `PageState` 三态包装 |

**禁止**：`el-empty` / `el-skeleton`（会把整套 Element Plus 拖进懒加载 chunk 且样式不同源）；渐变；彩色描边；`backdrop-blur` 叠层；hover 位移。

## 5. 布局

| 布局 | 组成 |
|---|---|
| 三栏（默认） | 会话侧栏 236px + 主区（自适应）+ 右栏面板 336/420px |
| 两栏 | 右栏收起时的主区铺满 |
| 设置两栏 | 左导航 208px（一级化，19 子项直接列出）+ 内容区 |
| 桌宠态 | 260×300 单窗口形态，可置顶、可移动、非命中区域鼠标穿透 |

**设置左导航一级化**：全部子项直接列出，分组标题只做视觉断行——避免「分组按钮 + 二级 chip」两跳才能到达目标页。

## 6. 交互决定

| 场景 | 设计 |
|---|---|
| 聊天过程 | **一条流水线**：工具调用是左侧连续竖轨 + 节点状态灯 + 等宽行文，一眼看出跑到第几步、哪步慢、哪步失败。不折叠成卡片堆 |
| 工具卡徽标 | 同工具连续失败显示 `×N`（失败计数）；达阈值显示「改道」（内核已注入替代路径）；exec 显示实际工作目录 |
| 审批 | 一张带上下文的卡：命令 / 工作目录 / 风险等级 / 已等待时长 + `拒绝 / 本会话允许 / 仍要批准`。拒绝是回执不是错误，模型会改道 |
| 输入区 | 一排放得下：模型 / 权限 / 参数 / 命令 / 引用 / 上下文环 / 发送；流式时自动变 `插入（steer） + 停止` 双键 |
| 右栏面板 | 工作区 / 变更 / 后台任务 / 辅助对话，同一位置、同一宽度、同一关闭方式 |
| 主题与背景 | 用户背景图提取的主色经 `applyExtractedPrimary()` 统一应用/移除（含强调色族派生）；禁止在别处直接 `style.setProperty('--wb-primary')` |

## 7. 关键界面

| 界面 | 要点 |
|---|---|
| 空态 · 新任务 | 草稿态不落库；示例卡可直接起一个任务 |
| 会话进行中 | 一轮完整 run：思维链折叠、工具时间线、变更聚合、用量徽标 |
| 流式执行中 | 工具运行中 + 正文边出边渲染；可插话、可排队、可停止 |
| 审批暂停 | 危险命令停下来问；跨重启后决策卡仍在 |
| 目标模式 | 目标卡 + 待办推进 + 计划胶囊；未达成自动续跑 |
| 中断与恢复 | 停止原因横幅 +「继续」从检查点续跑 |
| 设置中心 19 页 | 模型（模型服务 / 网络搜索）· 能力（工具 / 技能 / MCP / 子智能体 / 命令 / 钩子）· 数据（记忆 / 知识库 / 仓库导读 / 文件 / 仪表盘 / 运行历史）· 系统（外观 / 安全与高级 / 桌宠 / 文档 / 关于） |

## 8. 落地对应

| 原型 / 规范 | 前端实现 |
|---|---|
| `docs/prd/prototype/css/tokens.css` | `frontend/src/src/themes.css`（`--wb-*` 语义令牌） |
| `docs/prd/prototype/css/base.css` + `glass.css` | `frontend/src/src/wb-ui.css`（组件语法唯一实现） |
| `docs/prd/prototype/css/shell.css` | `wb-ui.css` 外壳段 + `App.vue` / `SettingsView.vue` |
| `docs/prd/prototype/css/screens.css` | `wb-ui.css` 聊天段 + `MessageBlocksRenderer.vue` / `ApprovalInline.vue` / `ChatInput.vue` |

组件开发细则见 [`docs/COMPONENT-GUIDELINES.md`](docs/COMPONENT-GUIDELINES.md)，页面结构见 [`docs/PAGE-STRUCTURE.md`](docs/PAGE-STRUCTURE.md)。
